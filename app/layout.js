export const metadata = {
  title: "DGA Heirs",
  description: "Founder Intelligence Hub"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
